endpoints zhttp -> http4s (for json use io.circe?)  (endpoints layer will need handlers)
add layer handlers ( Will need UserRepo)
add layer Repo (will need doobie?)
add dockerized DB and some scripts how to start it and there should be a table USER created
